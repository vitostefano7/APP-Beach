@startuml
title POST /strutture

actor Client
participant Router
participant createStruttura

Client -> Router: POST /strutture
Router -> createStruttura: createStruttura()
note right of createStruttura
body: { location, location, location }
auth: user
end note
createStruttura -> Strutture: create(...req.body, owner)
Strutture -> MongoDB: query
MongoDB --> Strutture: result
Strutture --> createStruttura: data
createStruttura --> Router: response
Router --> Client: JSON
@enduml

@startuml
skinparam Style strictuml
skinparam sequence {
  ParticipantPadding 30
  MessageAlign center
}

title DELETE /preferences/favorites/:strutturaId

actor Client
participant "Express Router" as Router
participant "requireAuth" as requireAuth
participant "removeFavoriteStruttura" as Controller
database MongoDB

Client -> Router: DELETE /preferences/favorites/:strutturaId
activate Router
Router -> requireAuth: execute
activate requireAuth
requireAuth --> Router: next()
deactivate requireAuth
Router -> Controller: removeFavoriteStruttura()
activate Controller

autonumber
Controller -> MongoDB: UserPreferences.find()
MongoDB --> Controller: result
note over Controller: Validate Input / Check Existence

alt success
  Controller --> Client: res.json(data)
else catch (error)
  Controller --> Client: res.status(500)
end

deactivate Controller
Router --> Client: HTTP Response
deactivate Router
@enduml

@startuml
skinparam Style strictuml
skinparam sequence {
  ParticipantPadding 30
  MessageAlign center
}

title GET /:id/calendar

actor Client
participant "Express Router" as Router
participant "getCampoCalendarByMonth" as Controller
database MongoDB

Client -> Router: GET /:id/calendar
activate Router
Router -> Controller: getCampoCalendarByMonth()
activate Controller

autonumber
Controller -> MongoDB: CampoCalendarDay.find()
MongoDB --> Controller: result
note over Controller: Validate Input / Check Existence

alt success
  Controller --> Client: res.json(data)
else catch (error)
  Controller --> Client: res.status(500)
end

deactivate Controller
Router --> Client: HTTP Response
deactivate Router
@enduml
